import Login from "@pages/login";
import React from "react";
interface route {
  path: string;
  component: React.ReactElement;
}
[];
const routesURl: route[] = [
  { path: "ddd", component: <Login /> },
  { path: "ddd", component: <Login /> },
  { path: "ddd", component: <Login /> },
];
